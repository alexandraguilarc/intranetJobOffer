<%= form_with(model: @postulation, local: true) do |f| %>
    <% if @postulation.errors.any? %>
        <div id= "error_explanation">
            <h2>Esta postulación no se pudo crear por las siguientes razones</h2>
                <ul>
                    <% @postulation.errors.full_messages.each do |msg|%>
                        <li><%= msg %></li>
                    <% end %>
                </ul>
        </div>
    <% end %>
    
    <%= f.hidden_field :joboffer_id, value: @joboffer.id %>
        <div class= "field">
            <%= f.label :message, as: "Mensaje" %>
            <%= f.text_field :message %>
        <div class = 'actions'>
            <%= f.submit %>
        </div>
    <% end %>